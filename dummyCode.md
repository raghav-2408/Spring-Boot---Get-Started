# This returns the list of crops.
```java
   // return crops.stream().map(N -> {

        //     CropRequestDTO dto = new CropRequestDTO();
            
        //     dto.setFarmerId(N.getFarmerId());
        //     dto.setFarmerName(N.getFarmerName());
        //     dto.setCropName(N.getCropName());
        //     dto.setCategory(N.getCategory());
        //     dto.setQuantity(N.getQuantity());
        //     dto.setPricePerKg(N.getPricePerKg());
        //     dto.setLocation(N.getLocation());

        //     return dto;

        // }).toList();
```
